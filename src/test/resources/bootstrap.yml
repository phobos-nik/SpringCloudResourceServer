spring:
  application:
    name: test-resource
  jpa:
    generateDdl: true
  cloud:
    bus:
      enabled: false
    discovery:
      enabled: false
    consul:
      enabled: false
      config:
        enabled: false
